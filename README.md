# AuRa

Android: модули `app`, `passwordgen` (Gradle, корень `settings.gradle.kts`).

Сайт и сервер: `deploy/AuRa-deploy` (см. `deploy/AuRa-deploy/DEPLOY_1REG.md` и `deploy/AuRa-deploy/server/README.md`).

Клон `meshtastic-firmware` в эту папку не коммитится; при необходимости: `git clone https://github.com/meshtastic/firmware meshtastic-firmware`.
